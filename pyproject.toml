[project]
name = "uav-emergency-dqn"
version = "0.1.0"
description = "基于DQN的多无人机协同搜索突发事件临机决策系统"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24",
    "scipy>=1.10",
    "matplotlib>=3.7",
    "torch>=2.0",
    "tqdm>=4.64",
    "pandas>=1.5",
]
